namespace WordLists {
    //% fixedInstance
    export const EmptyFilter: Bloom = new Bloom(null)
    
    //% block
    export function isWordInFilter(b: Bloom, w: string): number {
        return 0
    }
}
